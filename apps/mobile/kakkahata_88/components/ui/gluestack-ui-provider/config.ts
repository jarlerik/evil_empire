import { createConfig } from '@gluestack-ui/themed';

export const config = createConfig({
	tokens: {
		colors: {
			primary: '#0891B2',
			secondary: '#64748B',
			tertiary: '#CBD5E1'
		}
	},
	aliases: {
		colors: {
			primary: 'primary',
			secondary: 'secondary',
			tertiary: 'tertiary'
		}
	}
});
